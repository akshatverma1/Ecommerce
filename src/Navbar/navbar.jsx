import React from 'react';
import './navbar.css'; // Import the CSS file if it’s available
import { Link } from 'react-router-dom';
const Navbar = () => {
  return (
    <></>
  );
};

export default Navbar;
